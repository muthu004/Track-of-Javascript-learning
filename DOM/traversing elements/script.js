


const h1=document.querySelector(".container-2")
const next=h1.nextElementSibling
console.log(next);
const previous=h1.previousElementSibling
console.log(previous);

const comment=h1.nextSibling
console.log(h1.parentNode);
console.log(comment.parentNode); //Selecting parent node
console.log(h1.parentElement);
console.log(comment.parentElement);//Selecting parent element
